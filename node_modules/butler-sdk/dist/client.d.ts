import { ExtractionResultsDto, QueuesApi } from "./api";
export declare class Butler {
    protected queuesApi: QueuesApi;
    constructor(apiKey: string);
    extractDocument(queueId: string, file: any): Promise<ExtractionResultsDto>;
}
